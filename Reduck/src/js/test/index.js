require(['./../util/state'], function(require) {
    var content = require('./../util/state');
    //document.open();
    document.write('<h1>' + content + '</h1>');
    //document.close();
});
console.log(2);

